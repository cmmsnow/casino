package io.zipcoder.casino.Utilities;

import io.zipcoder.casino.Players.Player;
import io.zipcoder.casino.utilities.Output;
import io.zipcoder.casino.utilities.Switchboard;
import org.junit.Assert;
import org.junit.Test;
import org.mockito.Mockito;
import static org.mockito.Mockito.*;

public class SwitchboardTest {
    @Test
    public void TestSetPlayer(){
//        Output outputMock;
//        Switchboard switchboard;
//        String name = "Hades";
//        Boolean gambler = true;
//        Double wallet = 50.0;
//
//        outputMock = Mockito.mock(Output.class);
//        switchboard = new Switchboard();
//
//        //when(outputMock.welcomeMenu()).thenReturn(name);
//        when(outputMock.getPlayerName()).thenReturn(name);
//        when(outputMock.isGambler()).thenReturn(gambler);
//        when(outputMock.getWalletAmount()).thenReturn(wallet);
//
//        Player expected = new Player(wallet, name);
//        Player actual = switchboard.setPlayer();
//
//        Assert.assertEquals(expected, actual);
    }

    @Test
    public void TestGameChoice(){
//        Output outputMock;
//        Switchboard switchboard;
//        Integer choice = 1;
//
//        outputMock = Mockito.mock(Output.class);
//        switchboard = new Switchboard();
//        Boolean gambler = spy(true);
//
//
//        when(outputMock.allGameOptions()).thenReturn(choice);
//        when(outputMock.nonGamblingOptions()).thenReturn(choice);
//
//        Integer actual = switchboard.gameChoice();
//
//        Assert.assertEquals(choice, actual);
    }

    @Test
    public void TestGamblerMenuSorter(){}

    @Test
    public void TestNonGamblerMenuSorter(){}
}
